# V2.1 三项改动 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为物品管理系统增加图片点击放大、物品编号自动生成、存放位置快捷选项三项能力。

**Architecture:** 编号计算与位置记忆各自抽为独立纯逻辑模块（`src/lib/`），业务页面只负责调用与渲染；编号唯一性由数据库 UNIQUE 约束硬保证，前端在冲突时按"自动填号重试 / 手填号报错"分流处理。

**Tech Stack:** React 19 + TypeScript 6 + Vite 5 + Ant Design 6.4.4 + Supabase（PostgREST）

**Spec:** `docs/superpowers/specs/2026-09-14-v2.1-three-features-design.md`

## Global Constraints

- **不安装任何新依赖** —— 用户明确要求跳过 TDD，不引入测试框架
- **验证方式**：每项改动完成后执行 `npx tsc -b --noEmit` 确认类型通过；全部完成后执行 `npm run build`
- **提交策略**：用户要求**全部改动完成后统一提交一次**，不在每个任务后提交。设计文档、计划文档与代码一并纳入同一次提交
- **不得重构与三项改动无关的代码**
- **样式不得回归**：表格缩略图 44×44、卡片图高度 130 且 `objectFit: 'cover'`，原样保留
- **编号规则**：全局流水号，三位零填充（`001`…`013` → 下一个 `014`）；仅识别纯数字编号；**删除后不补位**
- **数据库变更必须先取得用户对 SQL 原文的确认**，不得擅自执行
- **Postgres 唯一约束冲突错误码**：`23505`
- 单文件不超过 350 行（`CLAUDE.md` 约定）；`ItemFormPage.tsx` 当前 241 行，本次改动后仍须合规

---

### Task 1: 编号计算纯逻辑模块

**Files:**
- Create: `src/lib/itemCode.ts`

**Interfaces:**
- Consumes: 无
- Produces: `nextCode(existingCodes: string[]): string` —— Task 4 调用

- [ ] **Step 1: 创建 `src/lib/itemCode.ts`**

```ts
/**
 * 物品编号计算 —— 纯逻辑模块，无副作用、无外部依赖。
 *
 * 编号规则：全局流水号，三位零填充（001、002 … 013、014）。
 * 仅识别纯数字编号；用户手打的非数字编号（如 "ABC"）不参与计算。
 * 删除物品后编号不补位 —— 编号只增不减，历史记录中提到的编号永远指向同一件物品。
 */

/**
 * 从现有编号列表中计算下一个可用编号。
 *
 * 边界行为：
 * - 传入空数组或无任何纯数字编号 → 返回 '001'
 * - 传入 ['001' … '013'] → 返回 '014'
 * - 最大值超过 999（如 '1000'）→ 返回 '1001'（padStart 只补不截）
 */
export function nextCode(existingCodes: string[]): string {
  let max = 0;
  for (const raw of existingCodes) {
    const code = raw.trim();
    if (!/^\d+$/.test(code)) continue;
    const n = Number(code);
    if (n > max) max = n;
  }
  return String(max + 1).padStart(3, '0');
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: 无输出（通过）

---

### Task 2: 存放位置常量与最近使用记忆

**Files:**
- Create: `src/lib/locations.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `QUICK_LOCATIONS: readonly ['综合楼304', '综合楼302']`
  - `getRecentLocations(): string[]`
  - `rememberLocation(location: string): void`
  —— 均由 Task 6 调用

- [ ] **Step 1: 创建 `src/lib/locations.ts`**

```ts
/**
 * 存放位置快捷选项与最近使用记忆。
 *
 * - QUICK_LOCATIONS 为固定常量，新增存放地点只需修改这一行
 * - 最近使用记录存于 localStorage，按用户实际保存的完整位置值记录
 * - 所有 localStorage 操作均包裹 try/catch：无痕模式等场景下静默降级，不影响录入主流程
 */

/** 固定快捷选项。新增存放地点只需在此数组追加。 */
export const QUICK_LOCATIONS = ['综合楼304', '综合楼302'] as const;

const STORAGE_KEY = 'scau_inventory_recent_locations';
const MAX_RECENT = 4;

/** 读取最近使用过的存放位置，最新的排在最前。读取失败时返回空数组。 */
export function getRecentLocations(): string[] {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return [];
    const parsed: unknown = JSON.parse(raw);
    if (!Array.isArray(parsed)) return [];
    return parsed
      .filter((v): v is string => typeof v === 'string' && v.trim() !== '')
      .slice(0, MAX_RECENT);
  } catch {
    return [];
  }
}

/** 记录一个存放位置。已存在则移到最前，最多保留 MAX_RECENT 个。 */
export function rememberLocation(location: string): void {
  const value = location.trim();
  if (!value) return;
  try {
    const others = getRecentLocations().filter((v) => v !== value);
    const next = [value, ...others].slice(0, MAX_RECENT);
    localStorage.setItem(STORAGE_KEY, JSON.stringify(next));
  } catch {
    // localStorage 不可用 —— 静默降级
  }
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: 无输出（通过）

---

### Task 3: 数据库变更（需用户确认 SQL 后执行）

**Files:**
- Modify: 线上 Supabase 数据库（`rdmrplrlyojwugcxbqmj`）

**Interfaces:**
- Consumes: 无
- Produces: `items.code` 上的 UNIQUE 约束 —— Task 4 的重试逻辑依赖它才有意义

- [ ] **Step 1: 把 SQL 原文交用户确认**

向用户展示以下两条 SQL，**取得明确同意后**再执行。不得擅自执行。

```sql
-- 1. 编号唯一约束：数据库层面杜绝重复编号
ALTER TABLE items ADD CONSTRAINT items_code_unique UNIQUE (code);

-- 2. 数据订正：008 木头凳子的位置写法与其余数据不一致
UPDATE items SET location = '综合楼304' WHERE code = '008' AND location = '304';
```

前置条件（已于设计阶段核实）：线上 13 条编号唯一、连续，无重复，约束可成功添加。

- [ ] **Step 2: 执行 SQL**

通过 Supabase SQL Editor 执行（用户操作，或经用户许可后由 AI 执行）。

- [ ] **Step 3: 验证约束生效**

Run:
```bash
KEY="sb_publishable_LMYWpxNTqc4klCZ_BSfaww_9FeAZBG9"
curl -s -X POST "https://rdmrplrlyojwugcxbqmj.supabase.co/rest/v1/items" \
  -H "apikey: $KEY" -H "Authorization: Bearer $KEY" \
  -H "Content-Type: application/json" \
  -d '{"id":"constraint-test","name":"约束测试","code":"001","category":"activity","quantity":1,"available_qty":1,"location":"test"}'
```
Expected: 返回错误 `duplicate key value violates unique constraint "items_code_unique"`

- [ ] **Step 4: 验证数据订正**

Run:
```bash
KEY="sb_publishable_LMYWpxNTqc4klCZ_BSfaww_9FeAZBG9"
curl -s "https://rdmrplrlyojwugcxbqmj.supabase.co/rest/v1/items?code=eq.008&select=code,location" \
  -H "apikey: $KEY" -H "Authorization: Bearer $KEY"
```
Expected: `[{"code":"008","location":"综合楼304"}]`

---

### Task 4: itemService — 编号查询与防撞重试

**Files:**
- Modify: `src/services/itemService.ts`（文件末尾追加，并改写 `createItem`）
- Modify: `src/hooks/useItems.ts:101-122`（`addItem` 透传 options）

**Interfaces:**
- Consumes: `nextCode` from `src/lib/itemCode.ts`（Task 1）
- Produces:
  - `fetchNextCode(): Promise<string>`
  - `createItem(input, options?: { autoCode?: boolean }): Promise<Item>`
  - `CODE_CONFLICT_MESSAGE: string`
  - `useItems().addItem(data, options?: { autoCode?: boolean }): Promise<void>`
  —— 均由 Task 5 调用

- [ ] **Step 1: 在 `src/services/itemService.ts` 顶部补充 import 与常量**

现有第 1-3 行：

```ts
import { supabase } from '../lib/supabase';
import type { Item, ItemCategory } from '../types';
import { cacheGet, cacheSet, cacheInvalidate, CACHE_KEYS } from '../lib/cache';
```

改为：

```ts
import { supabase } from '../lib/supabase';
import type { Item, ItemCategory } from '../types';
import { cacheGet, cacheSet, cacheInvalidate, CACHE_KEYS } from '../lib/cache';
import { nextCode } from '../lib/itemCode';

/** Postgres 唯一约束冲突错误码 */
const UNIQUE_VIOLATION = '23505';

/** 编号冲突时展示给用户的文案 */
export const CODE_CONFLICT_MESSAGE = '该编号已存在，请更换';

/** 自动编号因并发冲突时的最大重试次数 */
const MAX_AUTO_CODE_ATTEMPTS = 3;
```

- [ ] **Step 2: 新增 `fetchNextCode()`**

在 `fetchItemById` 之后、"变更（自动失效缓存）"分节之前插入：

```ts
/**
 * 查询当前最大编号并返回下一个可用编号。
 *
 * 刻意不使用 TTL 缓存：若读取缓存数据，刚删除物品后会算出已被占用的编号。
 */
export async function fetchNextCode(): Promise<string> {
  const { data, error } = await supabase.from('items').select('code');
  if (error) throw error;
  return nextCode((data ?? []).map((r) => r.code as string));
}
```

- [ ] **Step 3: 改写 `createItem`**

将现有 `createItem`（`src/services/itemService.ts:121-134`）整体替换为：

```ts
/**
 * 新建物品。
 *
 * options.autoCode 为 true 时（编号由系统自动填入），若因并发导致编号冲突，
 * 会自动重新取号并重试，用户无感。
 *
 * options.autoCode 为 false 时（用户手动指定编号），冲突直接抛出
 * CODE_CONFLICT_MESSAGE —— 绝不静默改号，否则用户输入 005 却存成 007。
 */
export async function createItem(
  input: Omit<Item, 'id' | 'createdAt' | 'updatedAt'> & { id: string; createdAt: string; updatedAt: string },
  options?: { autoCode?: boolean },
): Promise<Item> {
  const maxAttempts = options?.autoCode ? MAX_AUTO_CODE_ATTEMPTS : 1;
  let lastError: Error = new Error(CODE_CONFLICT_MESSAGE);

  for (let attempt = 0; attempt < maxAttempts; attempt++) {
    const code = attempt === 0 ? input.code : await fetchNextCode();
    const row = itemToRow({ ...input, code });
    const { data, error } = await supabase
      .from('items')
      .insert(row)
      .select()
      .single();

    if (!error) {
      invalidateItemsCache();
      return rowToItem(data as Record<string, unknown>);
    }
    if (error.code !== UNIQUE_VIOLATION) throw error;
    lastError = new Error(CODE_CONFLICT_MESSAGE);
  }

  throw lastError;
}
```

- [ ] **Step 4: 让 `useItems.addItem` 透传 options**

`src/hooks/useItems.ts` 第 101-122 行，将 `addItem` 改为：

```ts
  const addItem = useCallback(
    async (
      data: Omit<Item, 'id' | 'createdAt' | 'updatedAt'>,
      options?: { autoCode?: boolean },
    ) => {
      const now = nowISO();
      const optimisticItem: Item = {
        ...data,
        id: generateId(),
        createdAt: now,
        updatedAt: now,
      };

      setItems((prev) => [optimisticItem, ...prev]);

      try {
        await createItem(optimisticItem, options);
        await refresh();
      } catch (err) {
        setItems((prev) => prev.filter((item) => item.id !== optimisticItem.id));
        throw err;
      }
    },
    [refresh],
  );
```

- [ ] **Step 5: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: 无输出（通过）

---

### Task 5: ItemFormPage — 编号自动填充与错误可见性

**Files:**
- Modify: `src/pages/ItemFormPage.tsx`

**Interfaces:**
- Consumes: `fetchNextCode`、`CODE_CONFLICT_MESSAGE`（Task 4）；`useItems().addItem(data, options)`（Task 4）
- Produces: 无（终端页面）

**背景：** 现有 `handleSubmit`（`ItemFormPage.tsx:107-142`）调用 `addItem(data)` **未 await**，而 `addItem` 会 `throw`。结果是保存失败也照样提示成功并跳转，错误被完全吞掉。编号冲突提示必须建立在这个问题的修复之上。

- [ ] **Step 1: 补充 import 与 ref**

现有第 1 行：

```ts
import { useState, useEffect } from 'react';
```

改为：

```ts
import { useState, useEffect, useRef } from 'react';
```

现有第 10 行：

```ts
import { fetchItemById } from '../services/itemService';
```

改为：

```ts
import { fetchItemById, fetchNextCode } from '../services/itemService';
```

在 `const [loading, setLoading] = useState(false);`（第 24 行）之后新增：

```ts
  // 标记编号是否被用户手动编辑过 —— 决定冲突时是重试还是报错
  const codeEditedRef = useRef(false);
```

- [ ] **Step 2: 新增自动编号预填 effect**

在编辑模式的 `useEffect`（第 27-53 行）之后新增：

```tsx
  // 新建模式：异步预填下一个编号
  useEffect(() => {
    if (isEdit) return;

    let cancelled = false;
    fetchNextCode()
      .then((code) => {
        // 竞态保护：用户可能已在编号框输入内容，此时不得覆盖
        if (cancelled || codeEditedRef.current) return;
        form.setFieldValue('code', code);
      })
      .catch(() => {
        if (!cancelled) message.warning('自动编号获取失败，请手动填写');
      });

    return () => { cancelled = true; };
  }, [isEdit, form]);
```

- [ ] **Step 3: 删除伪编号生成，给编号输入框挂 onChange**

现有 `initialValues`（第 166-170 行）：

```tsx
            initialValues={{
              quantity: 1,
              category: 'fixed_assets' as ItemCategory,
              code: `WP${Date.now().toString(36).toUpperCase().slice(-6)}`,
            }}
```

改为（移除 `code` 一行，编号改由 effect 异步填入）：

```tsx
            initialValues={{
              quantity: 1,
              category: 'fixed_assets' as ItemCategory,
            }}
```

现有编号输入框（第 176-178 行）：

```tsx
            <Form.Item name="code" label="物品编号" rules={[{ required: true, message: '请输入物品编号' }]}>
              <Input placeholder="如：WP20240101" />
            </Form.Item>
```

改为：

```tsx
            <Form.Item name="code" label="物品编号" rules={[{ required: true, message: '请输入物品编号' }]}>
              <Input
                placeholder="自动生成，可手动修改"
                onChange={() => { codeEditedRef.current = true; }}
              />
            </Form.Item>
```

- [ ] **Step 4: 改写 `handleSubmit` 为异步并捕获错误**

将现有 `handleSubmit`（第 107-142 行）整体替换为：

```tsx
  const handleSubmit = async (values: Record<string, unknown>) => {
    setLoading(true);
    try {
      const existingItem = isEdit ? items.find((i) => i.id === id) : null;

      const data = {
        name: values.name as string,
        code: values.code as string,
        category: values.category as ItemCategory,
        quantity: values.quantity as number,
        availableQty: isEdit
          ? (values.quantity as number) - ((existingItem?.quantity || 0) - (existingItem?.availableQty || 0))
          : (values.quantity as number),
        location: values.location as string,
        // 保护原图：有新图用新图，否则保留数据库中的原图
        photo: photoBase64 || existingItem?.photo || undefined,
        notes: values.notes as string | undefined,
      };

      if (isEdit && id) {
        const existing = items.find((i) => i.id === id);
        const diff = (values.quantity as number) - (existing?.quantity || 0);
        await updateItem(id, {
          ...data,
          availableQty: (existing?.availableQty || 0) + diff,
        });
        message.success('物品信息已更新');
      } else {
        // autoCode 为 true 时冲突会自动重试；为 false 时冲突直接报错
        await addItem(data, { autoCode: !codeEditedRef.current });
        message.success('物品已添加');
      }

      navigate('/items');
    } catch (err) {
      message.error(err instanceof Error ? err.message : '保存失败，请重试');
    } finally {
      setLoading(false);
    }
  };
```

> 注：本步骤**不要**在此处调用 `rememberLocation` —— 该函数由 Task 6 引入，此时尚不存在。Task 6 Step 5 会把记录位置的那行补回来。

- [ ] **Step 5: 验证错误提示真的会出现**

启动开发服务器，在新建页手动把编号改成已存在的 `001`，提交。
Expected: 停在表单页，提示「该编号已存在，请更换」，**不跳转**

- [ ] **Step 6: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: 无输出（通过）

---

### Task 6: ItemFormPage — 存放位置快捷选项

**Files:**
- Modify: `src/pages/ItemFormPage.tsx`

**Interfaces:**
- Consumes: `QUICK_LOCATIONS`、`getRecentLocations`、`rememberLocation`（Task 2）
- Produces: 无（终端页面）

- [ ] **Step 1: 补充 import**

现有第 4-5 行已经从 antd 导入了 `Space`，**只需补上 `Tag`**；另需新增 `InputRef` 类型导入（给位置输入框挂 ref 用）：

```ts
import {
  Card, Form, Input, InputNumber, Select, Button, message, Typography, Space, Tag,
} from 'antd';
import type { InputRef } from 'antd';
```

在业务模块 import 区域（现有第 8-11 行附近）新增：

```ts
import { QUICK_LOCATIONS, getRecentLocations, rememberLocation } from '../lib/locations';
```

- [ ] **Step 2: 新增 ref 与最近位置状态**

在 `codeEditedRef` 声明之后新增：

```ts
  const locationInputRef = useRef<InputRef>(null);
  const [recentLocations, setRecentLocations] = useState<string[]>(() => getRecentLocations());
```

- [ ] **Step 3: 新增填入函数**

在 `handleSubmit` 之前新增：

```tsx
  /** 填入存放位置，并把光标定位到末尾，方便继续输入柜位后缀 */
  const fillLocation = (value: string) => {
    form.setFieldValue('location', value);
    const input = locationInputRef.current?.input;
    if (input) {
      input.focus();
      input.setSelectionRange(value.length, value.length);
    }
  };
```

- [ ] **Step 4: 替换位置字段，加挂 ref 与快捷按钮**

现有（第 188-190 行）：

```tsx
            <Form.Item name="location" label="存放位置" rules={[{ required: true, message: '请输入存放位置' }]}>
              <Input placeholder="如：综合楼302办公室 柜子A" />
            </Form.Item>
```

替换为：

```tsx
            <Form.Item
              name="location"
              label="存放位置"
              rules={[{ required: true, message: '请输入存放位置' }]}
              style={{ marginBottom: 8 }}
            >
              <Input ref={locationInputRef} placeholder="如：综合楼302办公室 柜子A" />
            </Form.Item>

            <div style={{ marginBottom: 24 }}>
              <Space size={4} wrap>
                {QUICK_LOCATIONS.map((loc) => (
                  <Tag
                    key={loc}
                    color="blue"
                    style={{ cursor: 'pointer', marginInlineEnd: 0 }}
                    onClick={() => fillLocation(loc)}
                  >
                    {loc}
                  </Tag>
                ))}
                {recentLocations
                  .filter((loc) => !(QUICK_LOCATIONS as readonly string[]).includes(loc))
                  .map((loc) => (
                    <Tag
                      key={loc}
                      style={{ cursor: 'pointer', marginInlineEnd: 0 }}
                      onClick={() => fillLocation(loc)}
                    >
                      {loc}
                    </Tag>
                  ))}
              </Space>
            </div>
```

- [ ] **Step 5: 保存成功后记录该位置**

在 `handleSubmit` 的成功路径中，`if (data.location) rememberLocation(data.location);` 之后补充刷新本地状态：

```ts
      if (data.location) {
        rememberLocation(data.location);
        setRecentLocations(getRecentLocations());
      }
```

- [ ] **Step 6: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: 无输出（通过）

---

### Task 7: ItemListPage — 图片点击放大

**Files:**
- Modify: `src/pages/ItemListPage.tsx`

**Interfaces:**
- Consumes: 无（使用 antd 内置 `<Image>`）
- Produces: 无（终端页面）

**背景：** 图片所在容器的事件会冒泡到外层 `<Card onClick>`（卡片视图）与 `<tr onClick>`（表格视图），导致点击图片**同时**打开预览并跳转。两个视图都必须在图片容器上拦截冒泡。

- [ ] **Step 1: 补充 import**

第 3-6 行的 antd import 中加入 `Image`：

```ts
import {
  Card, Input, Select, Row, Col, Tag, Button, Empty, Table, Space,
  Popconfirm, message, Typography, Badge, Spin, Result, Image,
} from 'antd';
```

第 7-11 行的图标 import 中加入 `EyeOutlined`：

```ts
import {
  SearchOutlined, PlusOutlined, EditOutlined, DeleteOutlined,
  SwapOutlined, AppstoreOutlined, UnorderedListOutlined,
  EnvironmentOutlined, DownloadOutlined, EyeOutlined,
} from '@ant-design/icons';
```

- [ ] **Step 2: 表格视图图片改为可预览**

将第 56-79 行的 `render` 中，`<motion.div>` 外层加 `onClick` 拦截冒泡，并把裸 `<img>` 换成 `<Image>`：

```tsx
      render: (photo: string) =>
        photo ? (
          <motion.div
            initial={{ scale: 0, opacity: 0 }}
            animate={{ scale: 1, opacity: 1 }}
            transition={{ type: 'spring', stiffness: 250, damping: 18 }}
            style={{ width: 44, height: 44, borderRadius: 8, overflow: 'hidden' }}
            onClick={(e) => e.stopPropagation()}
          >
            {/* Water drop overlay that fades away */}
            <motion.div
              initial={{ opacity: 1 }}
              animate={{ opacity: 0 }}
              transition={{ delay: 0.3, duration: 0.3 }}
              style={{
                position: 'absolute', inset: 0, zIndex: 1,
                background: 'radial-gradient(circle at center, #0EA5E9 0%, transparent 70%)',
                borderRadius: 8, pointerEvents: 'none',
              }}
            />
            <Image
              src={photo}
              alt=""
              width={44}
              height={44}
              style={{ borderRadius: 8, objectFit: 'cover', display: 'block' }}
              preview={{ mask: <EyeOutlined /> }}
            />
          </motion.div>
        ) : (
          <div style={{ width: 44, height: 44, borderRadius: 8, background: '#E0F2FE', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#0EA5E9', fontSize: 20 }}>📦</div>
        ),
```

- [ ] **Step 3: 卡片视图图片改为可预览**

将第 215-232 行的 `<motion.div>`（卡片封面动画容器）加 `onClick` 拦截冒泡，并把裸 `<img>` 换成 `<Image>`：

```tsx
                            <motion.div
                              initial={{ scale: 0.8, opacity: 0 }}
                              animate={{ scale: 1, opacity: 1 }}
                              transition={{ type: 'spring', stiffness: 200, damping: 16 }}
                              style={{ height: 130, overflow: 'hidden', position: 'relative' }}
                              onClick={(e) => e.stopPropagation()}
                            >
                              <motion.div
                                initial={{ opacity: 1 }}
                                animate={{ opacity: 0 }}
                                transition={{ delay: 0.35, duration: 0.25 }}
                                style={{
                                  position: 'absolute', inset: 0, zIndex: 1,
                                  background: 'radial-gradient(circle at center, #0EA5E9 0%, transparent 65%)',
                                  pointerEvents: 'none',
                                }}
                              />
                              <Image
                                src={item.photo}
                                alt={item.name}
                                width="100%"
                                height={130}
                                style={{ objectFit: 'cover', display: 'block' }}
                                preview={{ mask: <EyeOutlined /> }}
                              />
                            </motion.div>
```

- [ ] **Step 4: 类型检查**

Run: `npx tsc -b --noEmit`
Expected: 无输出（通过）

---

### Task 8: 构建验证与提交

**Files:**
- 全项目

- [ ] **Step 1: 生产构建**

Run: `npm run build`
Expected: 构建成功，产出 `dist/`，无 TypeScript 错误

- [ ] **Step 2: 生成构建产物对照**

Run:
```bash
ls -la dist/assets/ | head -20
```
Expected: 可见 `antd`、`recharts`、`framer`、`xlsx` 分包文件（`vite.config.ts` 的 `manualChunks` 生效）

- [ ] **Step 3: 交用户人工验收**

请用户启动 `npm run dev` 并逐项确认：

| # | 验收项 | 预期 |
|---|---|---|
| 1 | 卡片视图点击图片 | 打开大图预览，可缩放、旋转、下载 |
| 2 | 卡片视图点击图片以外的区域 | 正常跳转借用页 |
| 3 | 表格视图点击 44px 缩略图 | 打开大图预览 |
| 4 | 新建物品 | 编号自动填充为 `014` |
| 5 | 手动把编号改为已存在的 `001` 并提交 | 提示「该编号已存在，请更换」，**停留在表单页** |
| 6 | 点击位置快捷标签「综合楼304」 | 填入且光标停于末尾，可直接续打「左侧柜子」 |
| 7 | 保存后重新进入新建页 | 「最近用过」出现刚保存的完整位置 |
| 8 | 表格/卡片图片尺寸 | 与改动前一致，无布局错乱 |

- [ ] **Step 4: 用户确认后统一提交**

**必须等用户明确说可以提交后再执行。**

```bash
cd /Users/rlone/scau-water-inventory
git add -A
git commit -m "feat: V2.1 图片预览 + 自动编号 + 位置快捷选项

- 图片支持点击放大（antd Image 内置缩放/旋转/下载），修复点击图片误跳借用页
- 物品编号按录入顺序自动生成（001 起递增，不补位），保留手动修改
- items.code 加 UNIQUE 约束，自动编号并发冲突自动重试，手填编号冲突明确报错
- 存放位置快捷选项（综合楼304/302）+ 最近使用记忆
- 修复保存失败仍提示成功并跳转的错误吞噬问题
- 新增设计文档与实施计划文档"
```

- [ ] **Step 5: 推送**

**必须等用户明确同意后再推送。**

```bash
git push origin main
```
Expected: 推送成功，GitHub Actions 触发 GitHub Pages 自动部署

---

## 附录：不在本次范围内

以下为实施过程中发现但**不属于本次改动范围**的问题，已记录未处理：

- `ItemListPage.tsx:62` 的图片容器缺少 `position: relative`，导致其内部以 `position: absolute; inset: 0` 定位的水滴光晕动画可能相对于更外层元素定位，动画位置可能不符合预期。属既有视觉问题，与本次三项改动无关。
- `ItemFormPage.tsx` 中 base64 图片压缩上限为 250KB，而线上实际图片平均 73KB，未发现冲突。
